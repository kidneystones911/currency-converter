# Simple Currency Converter

This program gets the latest USD to BDT exchange rate and shows how much BDT you get for 100 USD.

## What it does
- Fetches live exchange rate from the internet
- Shows the result on screen
- Saves the data in a file called `exchange_rates.json`

## How to Run

1. Open your terminal / command prompt
2. Run this command once:
   ```bash
   pip install requests